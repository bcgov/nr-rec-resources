generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["metrics", "multiSchema", "typedSql", "views"]
  binaryTargets   = ["native", "debian-openssl-3.0.x", "linux-arm64-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["admin", "rst"]
}

model recreation_resource {
  rec_resource_id                      String                                 @id @db.VarChar(20)
  name                                 String?                                @db.VarChar(200)
  description                          String?
  closest_community                    String?                                @db.VarChar(200)
  display_on_public_site               Boolean?                               @default(false)
  district_code                        String?                                @db.VarChar(4)
  maintenance_standard_code            String?                                @db.VarChar(1)
  updated_at                           DateTime?                              @default(now()) @db.Timestamp(6)
  updated_by                           String?
  created_at                           DateTime?                              @default(now()) @db.Timestamp(6)
  created_by                           String?
  sys_period                           Unsupported("tstzrange")               @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  project_established_date             DateTime?                              @db.Date
  control_access_code                  String?                                @db.VarChar(1)
  risk_rating_code                     String?                                @db.VarChar(2)
  recreation_access                    recreation_access[]
  recreation_activity                  recreation_activity[]
  recreation_agreement_holder          recreation_agreement_holder?
  recreation_defined_campsite          recreation_defined_campsite[]
  recreation_driving_direction         recreation_driving_direction?
  recreation_establishment_order_docs  recreation_establishment_order_docs[]
  recreation_feature                   recreation_feature[]
  recreation_fee                       recreation_fee[]
  recreation_map_feature               recreation_map_feature[]
  recreation_control_access_code       recreation_control_access_code?        @relation(fields: [control_access_code], references: [recreation_control_access_code], onDelete: NoAction, onUpdate: NoAction)
  recreation_district_code             recreation_district_code?              @relation(fields: [district_code], references: [district_code], onDelete: NoAction, onUpdate: NoAction)
  recreation_maintenance_standard_code recreation_maintenance_standard_code?  @relation(fields: [maintenance_standard_code], references: [maintenance_standard_code], onDelete: NoAction, onUpdate: NoAction)
  recreation_risk_rating_code          recreation_risk_rating_code?           @relation(fields: [risk_rating_code], references: [risk_rating_code], onDelete: NoAction, onUpdate: NoAction)
  recreation_resource_docs             recreation_resource_docs[]
  recreation_resource_document         recreation_resource_document[]
  recreation_resource_image            recreation_resource_image[]
  recreation_resource_images           recreation_resource_images[]
  recreation_resource_reservation_info recreation_resource_reservation_info?
  recreation_site_description          recreation_site_description?
  recreation_status                    recreation_status?
  recreation_structure                 recreation_structure[]
  recreation_resource_type_view_admin  recreation_resource_type_view_admin[]
  recreation_resource_type_view_public recreation_resource_type_view_public[]

  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_activity {
  rec_resource_id          String                   @db.VarChar(200)
  recreation_activity_code Int
  updated_at               DateTime?                @default(now()) @db.Timestamp(6)
  updated_by               String?
  created_at               DateTime?                @default(now()) @db.Timestamp(6)
  created_by               String?
  sys_period               Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource      recreation_resource      @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)
  recreation_activity      recreation_activity_code @relation("recreation_activity", fields: [recreation_activity_code], references: [recreation_activity_code], onDelete: NoAction, onUpdate: NoAction)

  @@unique([rec_resource_id, recreation_activity_code])
  @@index([rec_resource_id], map: "idx_recreation_activity_rec_resource_id")
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_activity_code {
  recreation_activity_code Int                      @id @default(autoincrement())
  description              String?                  @db.VarChar(120)
  updated_at               DateTime?                @default(now()) @db.Timestamp(6)
  sys_period               Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_activity      recreation_activity[]    @relation("recreation_activity")

  @@schema("rst")
}

model recreation_status {
  rec_resource_id        String                   @id @db.VarChar(200)
  status_code            Int
  comment                String                   @db.VarChar(5000)
  updated_at             DateTime?                @default(now()) @db.Timestamp(6)
  updated_by             String?
  created_at             DateTime?                @default(now()) @db.Timestamp(6)
  created_by             String?
  sys_period             Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource    recreation_resource      @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)
  recreation_status_code recreation_status_code   @relation(fields: [status_code], references: [status_code], onDelete: NoAction, onUpdate: NoAction)

  @@index([rec_resource_id], map: "idx_recreation_status_rec_resource_id")
  @@schema("rst")
}

model recreation_status_code {
  status_code       Int                      @id @default(autoincrement())
  description       String                   @db.VarChar(120)
  updated_at        DateTime?                @default(now()) @db.Timestamp(6)
  sys_period        Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_status recreation_status[]

  @@schema("rst")
}

model recreation_resource_type_code {
  rec_resource_type_code String                   @id @db.VarChar(10)
  description            String                   @db.VarChar(200)
  updated_at             DateTime?                @default(now()) @db.Timestamp(6)
  sys_period             Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_map_feature recreation_map_feature[]

  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_district_code {
  district_code       String                   @id @db.VarChar(4)
  description         String                   @db.VarChar(120)
  updated_at          DateTime?                @default(now()) @db.Timestamp(6)
  sys_period          Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  is_archived         Boolean                  @default(false)
  recreation_resource recreation_resource[]

  @@schema("rst")
}

model recreation_fee {
  rec_resource_id     String                   @db.VarChar(200)
  fee_amount          Int?
  fee_start_date      DateTime?                @db.Date
  fee_end_date        DateTime?                @db.Date
  monday_ind          String?                  @default("N") @db.VarChar(1)
  tuesday_ind         String?                  @default("N") @db.VarChar(1)
  wednesday_ind       String?                  @default("N") @db.VarChar(1)
  thursday_ind        String?                  @default("N") @db.VarChar(1)
  friday_ind          String?                  @default("N") @db.VarChar(1)
  saturday_ind        String?                  @default("N") @db.VarChar(1)
  sunday_ind          String?                  @default("N") @db.VarChar(1)
  recreation_fee_code String                   @db.VarChar(1)
  updated_at          DateTime?                @default(now()) @db.Timestamp(6)
  updated_by          String?
  created_at          DateTime?                @default(now()) @db.Timestamp(6)
  created_by          String?
  sys_period          Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  fee_id              Int                      @id @default(autoincrement())
  recreation_resource recreation_resource      @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)
  with_description    recreation_fee_code      @relation("fee_with_description", fields: [recreation_fee_code], references: [recreation_fee_code], onDelete: NoAction, onUpdate: NoAction)

  @@unique([rec_resource_id, recreation_fee_code, monday_ind, tuesday_ind, wednesday_ind, thursday_ind, friday_ind, saturday_ind, sunday_ind], map: "recreation_fee_unique_idx")
  @@index([rec_resource_id], map: "idx_recreation_fee_rec_resource_id")
  @@schema("rst")
}

model recreation_fee_code {
  recreation_fee_code String                   @id @db.VarChar(1)
  description         String                   @db.VarChar(120)
  updated_at          DateTime?                @default(now()) @db.Timestamp(6)
  sys_period          Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  with_description    recreation_fee[]         @relation("fee_with_description")

  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_structure {
  rec_resource_id           String                    @db.VarChar(20)
  structure_code            Int
  updated_at                DateTime?                 @default(now()) @db.Timestamp(6)
  updated_by                String?
  created_at                DateTime?                 @default(now()) @db.Timestamp(6)
  created_by                String?
  sys_period                Unsupported("tstzrange")  @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource       recreation_resource       @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)
  recreation_structure_code recreation_structure_code @relation(fields: [structure_code], references: [structure_code], onDelete: NoAction, onUpdate: NoAction)

  @@unique([rec_resource_id, structure_code])
  @@index([rec_resource_id], map: "idx_recreation_structure_rec_resource_id")
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_structure_code {
  structure_code       Int                      @id @default(autoincrement())
  description          String?                  @db.VarChar(120)
  updated_at           DateTime?                @default(now()) @db.Timestamp(6)
  sys_period           Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_structure recreation_structure[]

  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_access {
  id                         Int                         @id @default(autoincrement())
  rec_resource_id            String                      @db.VarChar(20)
  access_code                String                      @db.VarChar(3)
  sub_access_code            String?                     @db.VarChar(3)
  updated_at                 DateTime?                   @default(now()) @db.Timestamp(6)
  updated_by                 String?
  created_at                 DateTime?                   @default(now()) @db.Timestamp(6)
  created_by                 String?
  sys_period                 Unsupported("tstzrange")    @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_access_code     recreation_access_code      @relation(fields: [access_code], references: [access_code], onDelete: NoAction, onUpdate: NoAction)
  recreation_resource        recreation_resource         @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)
  recreation_sub_access_code recreation_sub_access_code? @relation(fields: [sub_access_code], references: [sub_access_code], onDelete: NoAction, onUpdate: NoAction)

  @@unique([rec_resource_id, access_code, sub_access_code])
  @@index([rec_resource_id], map: "idx_recreation_access_rec_resource_id")
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_access_code {
  access_code       String                   @id @db.VarChar(3)
  description       String?                  @db.VarChar(120)
  sub_description   String?                  @db.VarChar(120)
  updated_at        DateTime?                @default(now()) @db.Timestamp(6)
  sys_period        Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_access recreation_access[]

  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_sub_access_code {
  sub_access_code   String                   @id @db.VarChar(3)
  description       String?                  @db.VarChar(120)
  updated_at        DateTime?                @default(now()) @db.Timestamp(6)
  sys_period        Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_access recreation_access[]

  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_map_feature {
  rmf_skey                      Int                            @id
  rec_resource_id               String?                        @db.VarChar(10)
  section_id                    String?                        @db.VarChar(30)
  amendment_id                  Int?
  amend_status_code             String?                        @db.VarChar(3)
  recreation_resource_type      String?                        @db.VarChar(3)
  amend_status_date             DateTime?                      @db.Date
  retirement_date               DateTime?                      @db.Date
  revision_count                Int?
  recreation_map_feature_guid   String?                        @db.VarChar(36)
  updated_at                    DateTime?                      @default(now()) @db.Timestamp(6)
  updated_by                    String?
  created_at                    DateTime?                      @default(now()) @db.Timestamp(6)
  created_by                    String?
  sys_period                    Unsupported("tstzrange")       @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource           recreation_resource?           @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)
  recreation_resource_type_code recreation_resource_type_code? @relation(fields: [recreation_resource_type], references: [rec_resource_type_code], onDelete: NoAction, onUpdate: NoAction)
  recreation_map_feature_geom   recreation_map_feature_geom?

  @@unique([rec_resource_id, section_id])
  @@index([rec_resource_id], map: "idx_recreation_map_feature_rec_resource_id")
  @@index([recreation_resource_type], map: "idx_recreation_map_feature_recreation_resource_type")
  @@index([rmf_skey], map: "idx_recreation_map_feature_rmf_skey")
  @@schema("rst")
}

model recreation_map_feature_geom {
  rmf_skey               Int                      @id
  map_feature_id         Int?                     @unique
  geometry_type_code     String?                  @db.VarChar(3)
  geometry               Unsupported("geometry")?
  feature_area           Decimal?                 @db.Decimal(11, 4)
  feature_length         Decimal?                 @db.Decimal(11, 4)
  feature_perimeter      Decimal?                 @db.Decimal(11, 4)
  revision_count         Int?
  updated_at             DateTime?                @default(now()) @db.Timestamp(6)
  updated_by             String?
  created_at             DateTime?                @default(now()) @db.Timestamp(6)
  created_by             String?
  sys_period             Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_map_feature recreation_map_feature   @relation(fields: [rmf_skey], references: [rmf_skey], onDelete: NoAction)

  @@index([map_feature_id], map: "idx_recreation_map_feature_geom_map_feature_id")
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_access_code_history {
  access_code     String                   @db.VarChar(3)
  description     String?                  @db.VarChar(120)
  sub_description String?                  @db.VarChar(120)
  updated_at      DateTime?                @db.Timestamp(6)
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_access_history {
  id              Int
  rec_resource_id String                   @db.VarChar(20)
  access_code     String                   @db.VarChar(3)
  sub_access_code String?                  @db.VarChar(3)
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_activity_code_history {
  recreation_activity_code Int
  description              String?                  @db.VarChar(120)
  updated_at               DateTime?                @db.Timestamp(6)
  sys_period               Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_activity_history {
  rec_resource_id          String                   @db.VarChar(200)
  recreation_activity_code Int
  updated_at               DateTime?                @db.Timestamp(6)
  updated_by               String?
  created_at               DateTime?                @db.Timestamp(6)
  created_by               String?
  sys_period               Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_district_code_history {
  district_code String                   @db.VarChar(4)
  description   String                   @db.VarChar(120)
  updated_at    DateTime?                @db.Timestamp(6)
  sys_period    Unsupported("tstzrange")
  is_archived   Boolean                  @default(false)

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_fee_code_history {
  recreation_fee_code String                   @db.VarChar(1)
  description         String                   @db.VarChar(120)
  updated_at          DateTime?                @db.Timestamp(6)
  sys_period          Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_fee_history {
  rec_resource_id     String                   @db.VarChar(200)
  fee_amount          Int?
  fee_start_date      DateTime?                @db.Date
  fee_end_date        DateTime?                @db.Date
  monday_ind          String?                  @db.VarChar(1)
  tuesday_ind         String?                  @db.VarChar(1)
  wednesday_ind       String?                  @db.VarChar(1)
  thursday_ind        String?                  @db.VarChar(1)
  friday_ind          String?                  @db.VarChar(1)
  saturday_ind        String?                  @db.VarChar(1)
  sunday_ind          String?                  @db.VarChar(1)
  recreation_fee_code String                   @db.VarChar(1)
  updated_at          DateTime?                @db.Timestamp(6)
  updated_by          String?
  created_at          DateTime?                @db.Timestamp(6)
  created_by          String?
  sys_period          Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_map_feature_geom_history {
  rmf_skey           Int
  map_feature_id     Int?
  geometry_type_code String?                  @db.VarChar(3)
  geometry           Unsupported("geometry")?
  feature_area       Decimal?                 @db.Decimal(11, 4)
  feature_length     Decimal?                 @db.Decimal(11, 4)
  feature_perimeter  Decimal?                 @db.Decimal(11, 4)
  revision_count     Int?
  updated_at         DateTime?                @db.Timestamp(6)
  updated_by         String?
  created_at         DateTime?                @db.Timestamp(6)
  created_by         String?
  sys_period         Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_map_feature_history {
  rmf_skey                    Int
  rec_resource_id             String?                  @db.VarChar(10)
  section_id                  String?                  @db.VarChar(30)
  amendment_id                Int?
  amend_status_code           String?                  @db.VarChar(3)
  recreation_resource_type    String?                  @db.VarChar(3)
  amend_status_date           DateTime?                @db.Date
  retirement_date             DateTime?                @db.Date
  revision_count              Int?
  recreation_map_feature_guid String?                  @db.VarChar(36)
  updated_at                  DateTime?                @db.Timestamp(6)
  updated_by                  String?
  created_at                  DateTime?                @db.Timestamp(6)
  created_by                  String?
  sys_period                  Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_resource_history {
  rec_resource_id           String                   @db.VarChar(20)
  name                      String?                  @db.VarChar(200)
  description               String?
  closest_community         String?                  @db.VarChar(200)
  display_on_public_site    Boolean?
  district_code             String?                  @db.VarChar(4)
  maintenance_standard_code String?                  @db.VarChar(1)
  updated_at                DateTime?                @db.Timestamp(6)
  updated_by                String?
  created_at                DateTime?                @db.Timestamp(6)
  created_by                String?
  sys_period                Unsupported("tstzrange")
  project_established_date  DateTime?                @db.Date
  control_access_code       String?                  @db.VarChar(1)
  risk_rating_code          String?                  @db.VarChar(2)

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_resource_type_code_history {
  rec_resource_type_code String                   @db.VarChar(10)
  description            String                   @db.VarChar(200)
  updated_at             DateTime?                @db.Timestamp(6)
  sys_period             Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_status_code_history {
  status_code Int
  description String                   @db.VarChar(120)
  updated_at  DateTime?                @db.Timestamp(6)
  sys_period  Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_status_history {
  rec_resource_id String                   @db.VarChar(200)
  status_code     Int
  comment         String                   @db.VarChar(5000)
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_structure_code_history {
  structure_code Int
  description    String?                  @db.VarChar(120)
  updated_at     DateTime?                @db.Timestamp(6)
  sys_period     Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_structure_history {
  rec_resource_id String                   @db.VarChar(20)
  structure_code  Int
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_sub_access_code_history {
  sub_access_code String                   @db.VarChar(3)
  description     String?                  @db.VarChar(120)
  updated_at      DateTime?                @db.Timestamp(6)
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

model recreation_resource_image_variants {
  ref_id                     String                     @db.VarChar
  size_code                  String                     @db.VarChar(20)
  url                        String?
  width                      Int?
  height                     Int?
  extension                  String?                    @db.VarChar(10)
  updated_at                 DateTime?                  @default(now()) @db.Timestamp(6)
  updated_by                 String?
  created_at                 DateTime?                  @default(now()) @db.Timestamp(6)
  created_by                 String?
  sys_period                 Unsupported("tstzrange")   @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource_images recreation_resource_images @relation(fields: [ref_id], references: [ref_id], onDelete: NoAction, onUpdate: NoAction)

  @@id([ref_id, size_code])
  @@schema("rst")
}

model recreation_resource_images {
  rec_resource_id                    String                               @db.VarChar(10)
  ref_id                             String                               @unique @db.VarChar
  caption                            String?                              @db.VarChar
  updated_at                         DateTime?                            @default(now()) @db.Timestamp(6)
  updated_by                         String?
  created_at                         DateTime?                            @default(now()) @db.Timestamp(6)
  created_by                         String?
  sys_period                         Unsupported("tstzrange")             @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource_image_variants recreation_resource_image_variants[]
  recreation_resource                recreation_resource                  @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)

  @@id([rec_resource_id, ref_id])
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_resource_doc_code {
  doc_code                     String                         @id @db.VarChar
  description                  String?                        @db.VarChar
  updated_at                   DateTime?                      @default(now()) @db.Timestamp(6)
  updated_by                   String?
  created_at                   DateTime?                      @default(now()) @db.Timestamp(6)
  created_by                   String?
  sys_period                   Unsupported("tstzrange")       @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource_docs     recreation_resource_docs[]
  recreation_resource_document recreation_resource_document[]

  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_resource_docs {
  ref_id                       String                        @id @db.VarChar
  rec_resource_id              String?                       @db.VarChar(10)
  title                        String?                       @db.VarChar
  url                          String?                       @db.VarChar
  doc_code                     String?                       @db.VarChar
  extension                    String?                       @db.VarChar
  updated_at                   DateTime?                     @default(now()) @db.Timestamp(6)
  updated_by                   String?
  created_at                   DateTime?                     @default(now()) @db.Timestamp(6)
  created_by                   String?
  sys_period                   Unsupported("tstzrange")      @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource_doc_code recreation_resource_doc_code? @relation(fields: [doc_code], references: [doc_code], onDelete: NoAction, onUpdate: NoAction, map: "recreation_resource_doc_doc_code_fkey")
  recreation_resource          recreation_resource?          @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction, map: "recreation_resource_doc_rec_resource_id_fkey")

  @@unique([rec_resource_id, ref_id])
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_establishment_order_docs {
  s3_key              String                   @id @db.VarChar
  rec_resource_id     String?                  @db.VarChar(10)
  title               String?                  @db.VarChar
  file_size           BigInt?
  extension           String?                  @db.VarChar
  updated_at          DateTime?                @default(now()) @db.Timestamp(6)
  updated_by          String?
  created_at          DateTime?                @default(now()) @db.Timestamp(6)
  created_by          String?
  sys_period          Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource recreation_resource?     @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)

  @@index([rec_resource_id], map: "idx_recreation_establishment_order_docs_rec_resource_id")
  @@schema("rst")
}

model flyway_schema_history {
  installed_rank Int      @id(map: "flyway_schema_history_pk")
  version        String?  @db.VarChar(50)
  description    String   @db.VarChar(200)
  type           String   @db.VarChar(20)
  script         String   @db.VarChar(1000)
  checksum       Int?
  installed_by   String   @db.VarChar(100)
  installed_on   DateTime @default(now()) @db.Timestamp(6)
  execution_time Int
  success        Boolean

  @@index([success], map: "flyway_schema_history_s_idx")
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_resource_doc_code_history {
  doc_code    String                   @db.VarChar
  description String?                  @db.VarChar
  updated_at  DateTime?                @db.Timestamp(6)
  updated_by  String?
  created_at  DateTime?                @db.Timestamp(6)
  created_by  String?
  sys_period  Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_resource_docs_history {
  ref_id          String                   @db.VarChar
  rec_resource_id String?                  @db.VarChar(10)
  title           String?                  @db.VarChar
  url             String?                  @db.VarChar
  doc_code        String?                  @db.VarChar
  extension       String?                  @db.VarChar
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_resource_image_variants_history {
  ref_id     String                   @db.VarChar
  size_code  String                   @db.VarChar(20)
  url        String?
  width      Int?
  height     Int?
  extension  String?                  @db.VarChar(10)
  updated_at DateTime?                @db.Timestamp(6)
  updated_by String?
  created_at DateTime?                @db.Timestamp(6)
  created_by String?
  sys_period Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_resource_images_history {
  rec_resource_id String                   @db.VarChar(10)
  ref_id          String                   @db.VarChar
  caption         String?                  @db.VarChar
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_site_point {
  rec_resource_id String                   @id @db.VarChar(10)
  geometry        Unsupported("geometry")?
  revision_count  Int?
  updated_at      DateTime?                @default(now()) @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @default(now()) @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))

  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_site_point_history {
  rec_resource_id String                   @db.VarChar(10)
  geometry        Unsupported("geometry")?
  revision_count  Int?
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_defined_campsite {
  rec_resource_id                                                                                                     String                        @db.VarChar(10)
  campsite_number                                                                                                     Int
  estimated_repair_cost                                                                                               Decimal?                      @db.Decimal(10, 2)
  recreation_remed_repair_code                                                                                        String?                       @db.VarChar(2)
  repair_complete_date                                                                                                DateTime?                     @db.Date
  updated_at                                                                                                          DateTime?                     @default(now()) @db.Timestamp(6)
  updated_by                                                                                                          String?
  created_at                                                                                                          DateTime?                     @default(now()) @db.Timestamp(6)
  created_by                                                                                                          String?
  sys_period                                                                                                          Unsupported("tstzrange")      @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource                                                                                                 recreation_resource           @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)
  recreation_remed_repair_code_recreation_defined_campsite_recreation_remed_repair_codeTorecreation_remed_repair_code recreation_remed_repair_code? @relation("recreation_defined_campsite_recreation_remed_repair_codeTorecreation_remed_repair_code", fields: [recreation_remed_repair_code], references: [recreation_remed_repair_code], onDelete: NoAction, onUpdate: NoAction)

  @@unique([rec_resource_id, campsite_number])
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_defined_campsite_history {
  rec_resource_id              String                   @db.VarChar(10)
  campsite_number              Int
  estimated_repair_cost        Decimal?                 @db.Decimal(10, 2)
  recreation_remed_repair_code String?                  @db.VarChar(2)
  repair_complete_date         DateTime?                @db.Date
  updated_at                   DateTime?                @db.Timestamp(6)
  updated_by                   String?
  created_at                   DateTime?                @db.Timestamp(6)
  created_by                   String?
  sys_period                   Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_driving_direction {
  rec_resource_id     String                   @id @db.VarChar(10)
  description         String?
  updated_at          DateTime?                @default(now()) @db.Timestamp(6)
  updated_by          String?
  created_at          DateTime?                @default(now()) @db.Timestamp(6)
  created_by          String?
  sys_period          Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource recreation_resource      @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_driving_direction_history {
  rec_resource_id String                   @db.VarChar(10)
  description     String?
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_maintenance_standard_code {
  maintenance_standard_code String                   @id @db.VarChar(1)
  description               String                   @db.VarChar(200)
  updated_at                DateTime?                @default(now()) @db.Timestamp(6)
  sys_period                Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource       recreation_resource[]

  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_maintenance_standard_code_history {
  maintenance_standard_code String                   @db.VarChar(1)
  description               String                   @db.VarChar(200)
  updated_at                DateTime?                @db.Timestamp(6)
  sys_period                Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_remed_repair_code {
  recreation_remed_repair_code                                                                                       String                        @id @db.VarChar(2)
  description                                                                                                        String?                       @db.VarChar(120)
  updated_at                                                                                                         DateTime?                     @default(now()) @db.Timestamp(6)
  sys_period                                                                                                         Unsupported("tstzrange")      @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_defined_campsite_recreation_defined_campsite_recreation_remed_repair_codeTorecreation_remed_repair_code recreation_defined_campsite[] @relation("recreation_defined_campsite_recreation_remed_repair_codeTorecreation_remed_repair_code")

  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_remed_repair_code_history {
  recreation_remed_repair_code String                   @db.VarChar(2)
  description                  String?                  @db.VarChar(120)
  updated_at                   DateTime?                @db.Timestamp(6)
  sys_period                   Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_site_description {
  rec_resource_id     String                   @id @db.VarChar(10)
  description         String?
  updated_at          DateTime?                @default(now()) @db.Timestamp(6)
  updated_by          String?
  created_at          DateTime?                @default(now()) @db.Timestamp(6)
  created_by          String?
  sys_period          Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource recreation_resource      @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_site_description_history {
  rec_resource_id String                   @db.VarChar(10)
  description     String?
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_agreement_holder {
  rec_resource_id      String                   @id @db.VarChar(10)
  client_number        String?                  @db.VarChar(8)
  agreement_start_date DateTime?                @db.Date
  agreement_end_date   DateTime?                @db.Date
  revision_count       Int?
  updated_at           DateTime?                @default(now()) @db.Timestamp(6)
  updated_by           String?
  created_at           DateTime?                @default(now()) @db.Timestamp(6)
  created_by           String?
  sys_period           Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource  recreation_resource      @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_agreement_holder_history {
  rec_resource_id      String                   @db.VarChar(10)
  client_number        String?                  @db.VarChar(8)
  agreement_start_date DateTime?                @db.Date
  agreement_end_date   DateTime?                @db.Date
  revision_count       Int?
  updated_at           DateTime?                @db.Timestamp(6)
  updated_by           String?
  created_at           DateTime?                @db.Timestamp(6)
  created_by           String?
  sys_period           Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_resource_reservation_info {
  rec_resource_id          String                   @id @db.VarChar(10)
  reservation_instructions String?                  @db.VarChar(200)
  reservation_website      String?                  @db.VarChar(200)
  reservation_phone_number String?                  @db.VarChar(50)
  reservation_email        String?                  @db.VarChar(100)
  reservation_comments     String?                  @db.VarChar(400)
  updated_at               DateTime?                @default(now()) @db.Timestamp(6)
  updated_by               String?
  created_at               DateTime?                @default(now()) @db.Timestamp(6)
  created_by               String?
  sys_period               Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource      recreation_resource      @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_resource_reservation_info_history {
  rec_resource_id          String                   @db.VarChar(10)
  reservation_instructions String?                  @db.VarChar(200)
  reservation_website      String?                  @db.VarChar(200)
  reservation_phone_number String?                  @db.VarChar(50)
  reservation_email        String?                  @db.VarChar(100)
  reservation_comments     String?                  @db.VarChar(400)
  updated_at               DateTime?                @db.Timestamp(6)
  updated_by               String?
  created_at               DateTime?                @db.Timestamp(6)
  created_by               String?
  sys_period               Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

model recreation_control_access_code {
  recreation_control_access_code String                   @id @db.VarChar(1)
  description                    String?                  @db.VarChar(120)
  effective_date                 DateTime?                @db.Date
  expiry_date                    DateTime?                @db.Date
  update_timestamp               DateTime?                @db.Timestamp(6)
  updated_at                     DateTime?                @default(now()) @db.Timestamp(6)
  updated_by                     String?
  created_at                     DateTime?                @default(now()) @db.Timestamp(6)
  created_by                     String?
  sys_period                     Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource            recreation_resource[]

  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_control_access_code_history {
  recreation_control_access_code String                   @db.VarChar(1)
  description                    String?                  @db.VarChar(120)
  effective_date                 DateTime?                @db.Date
  expiry_date                    DateTime?                @db.Date
  update_timestamp               DateTime?                @db.Timestamp(6)
  updated_at                     DateTime?                @db.Timestamp(6)
  updated_by                     String?
  created_at                     DateTime?                @db.Timestamp(6)
  created_by                     String?
  sys_period                     Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_establishment_order_docs_history {
  s3_key          String                   @db.VarChar
  rec_resource_id String?                  @db.VarChar(10)
  title           String?                  @db.VarChar
  file_size       BigInt?
  extension       String?                  @db.VarChar
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_risk_rating_code {
  risk_rating_code    String                   @id @db.VarChar(2)
  description         String                   @db.VarChar(100)
  updated_at          DateTime?                @default(now()) @db.Timestamp(6)
  sys_period          Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource recreation_resource[]

  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_risk_rating_code_history {
  risk_rating_code String                   @db.VarChar(2)
  description      String                   @db.VarChar(100)
  updated_at       DateTime?                @db.Timestamp(6)
  sys_period       Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_access_and_sub_access_code {
  access_code                 String                   @db.VarChar(3)
  access_code_description     String                   @db.VarChar(100)
  sub_access_code             String                   @db.VarChar(3)
  sub_access_code_description String?                  @db.VarChar(100)
  updated_at                  DateTime?                @default(now()) @db.Timestamp(6)
  sys_period                  Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))

  @@id([access_code, sub_access_code])
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_access_and_sub_access_code_history {
  access_code                 String                   @db.VarChar(3)
  access_code_description     String                   @db.VarChar(100)
  sub_access_code             String                   @db.VarChar(3)
  sub_access_code_description String?                  @db.VarChar(100)
  updated_at                  DateTime?                @db.Timestamp(6)
  sys_period                  Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_feature {
  rec_resource_id             String                   @db.VarChar(20)
  recreation_feature_code     String                   @db.VarChar(3)
  updated_at                  DateTime?                @default(now()) @db.Timestamp(6)
  updated_by                  String?
  created_at                  DateTime?                @default(now()) @db.Timestamp(6)
  created_by                  String?
  sys_period                  Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource         recreation_resource      @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)
  recreation_feature_code_ref recreation_feature_code  @relation("recreation_feature_code_ref", fields: [recreation_feature_code], references: [recreation_feature_code], onDelete: NoAction, onUpdate: NoAction)

  @@unique([rec_resource_id, recreation_feature_code])
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_feature_code {
  recreation_feature_code String                   @id @db.VarChar(3)
  description             String                   @db.VarChar(120)
  updated_at              DateTime?                @default(now()) @db.Timestamp(6)
  sys_period              Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_feature      recreation_feature[]     @relation("recreation_feature_code_ref")

  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_feature_code_history {
  recreation_feature_code String                   @db.VarChar(3)
  description             String                   @db.VarChar(120)
  updated_at              DateTime?                @db.Timestamp(6)
  sys_period              Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_feature_history {
  rec_resource_id         String                   @db.VarChar(20)
  recreation_feature_code String                   @db.VarChar(3)
  updated_at              DateTime?                @db.Timestamp(6)
  updated_by              String?
  created_at              DateTime?                @db.Timestamp(6)
  created_by              String?
  sys_period              Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_resource_document {
  doc_id                       String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  rec_resource_id              String                       @db.VarChar(10)
  doc_code                     String                       @db.VarChar
  file_name                    String                       @db.VarChar
  extension                    String                       @db.VarChar
  file_size                    BigInt?
  updated_at                   DateTime?                    @default(now()) @db.Timestamp(6)
  updated_by                   String?
  created_at                   DateTime?                    @default(now()) @db.Timestamp(6)
  created_by                   String?
  sys_period                   Unsupported("tstzrange")     @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource_doc_code recreation_resource_doc_code @relation(fields: [doc_code], references: [doc_code], onDelete: NoAction, onUpdate: NoAction)
  recreation_resource          recreation_resource          @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)

  @@index([rec_resource_id], map: "idx_recreation_resource_document_rec_resource_id")
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_resource_document_history {
  doc_id          String                   @db.Uuid
  rec_resource_id String                   @db.VarChar(10)
  doc_code        String                   @db.VarChar
  file_name       String                   @db.VarChar
  extension       String                   @db.VarChar
  file_size       BigInt?
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model recreation_resource_image {
  image_id            String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  rec_resource_id     String                   @db.VarChar(10)
  file_name           String                   @db.VarChar
  extension           String                   @db.VarChar
  file_size           BigInt?
  updated_at          DateTime?                @default(now()) @db.Timestamp(6)
  updated_by          String?
  created_at          DateTime?                @default(now()) @db.Timestamp(6)
  created_by          String?
  sys_period          Unsupported("tstzrange") @default(dbgenerated("tstzrange(CURRENT_TIMESTAMP, NULL::timestamp with time zone)"))
  recreation_resource recreation_resource      @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction)

  @@index([rec_resource_id], map: "idx_recreation_resource_image_rec_resource_id")
  @@schema("rst")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model recreation_resource_image_history {
  image_id        String                   @db.Uuid
  rec_resource_id String                   @db.VarChar(10)
  file_name       String                   @db.VarChar
  extension       String                   @db.VarChar
  file_size       BigInt?
  updated_at      DateTime?                @db.Timestamp(6)
  updated_by      String?
  created_at      DateTime?                @db.Timestamp(6)
  created_by      String?
  sys_period      Unsupported("tstzrange")

  @@ignore
  @@schema("rst")
}

/// The underlying view does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This view or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
view recreation_resource_type_view_admin {
  rec_resource_id        String               @unique @db.VarChar(20)
  rec_resource_type_code String?              @db.VarChar(3)
  description            String?              @db.VarChar(200)
  recreation_resource    recreation_resource? @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction, map: "recreation_resource_type_view_admin_recreation_resource_fkey")

  @@schema("rst")
}

view recreation_resource_type_view_public {
  rec_resource_id        String               @unique @db.VarChar(20)
  rec_resource_type_code String?              @db.VarChar
  description            String?              @db.VarChar(200)
  recreation_resource    recreation_resource? @relation(fields: [rec_resource_id], references: [rec_resource_id], onDelete: NoAction, onUpdate: NoAction, map: "recreation_resource_type_view_public_recreation_resource_fkey")

  @@schema("rst")
}
