/**
 * Maximum file size for image uploads (2MB).
 * This applies to all image variants (original, scr, pre, thm) and consent forms.
 */
export const MAX_FILE_SIZE = 2 * 1024 * 1024;

/**
 * Maximum number of files allowed per upload (original, scr, pre, thm, consent_form).
 */
export const MAX_FILES_COUNT = 5;
